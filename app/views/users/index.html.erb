<div class="container"> 
    <div class="row"> 
      <div class="col-lg-4">
       <%= render 'users/info', user: @user %>
      </div>
      <div class="col-lg-8"> 
        <table class="table table-basic">
          <tr>
          　<th>image</th>
            <th>name</th>  
            <th></th>
          </tr>
          <h3>Users</h3>
          <% @users.each do |user| %>
            <tr>
              <td>*image後でつくる</td>
              <td><%= user.name %></td>
              <td><%= link_to 'show', user_path(user.id) %></td>
            </tr>
          <% end %>
        </table>
      </div>
   </div>  
</div>  