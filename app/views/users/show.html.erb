<div class="container"> 
    <div class="row"> 
      <div class="col-lg-4">
       <%= render 'users/info', user: @user, book: @book %>
      </div>
      <div class="col-lg-8"> 
        <table class="table table-basic">
          <tr>
            <th></th>
          　<th>Title</th>
            <th>opinion</th>  
          </tr>
          <h3>Books</h3>
            <tr>
              <td><%= image_tag attachment_url(@user, :profile_image, :fill, 50, 50, format: "jpg") %></td>
              <td><%= @book.title %></td>
              <td><%= @book.body %></td>
            </tr>
          
        </table>
      </div>
   </div>  
</div>  