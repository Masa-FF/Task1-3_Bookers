<div class="container"> 
    <div class="row"> 
      <div class="col-xs-3">
       <%= render 'users/info', user: @user, book: @newbook %>
      </div>
      <div class="col-xs-9"> 
        <table class="table table-basic">
          <tr>
            <th></th>
          　<th>title</th>
            <th>Opinion</th>  
            <th></th>
            <th></th>
          </tr>
          <h3>Book detail</h3>
            <tr>
              <td>
                <%= attachment_image_tag @user, :profile_image , :fill, 50, 50, format: "jpg", fallback: "no_image.jpg", size:'50x50' %>
                <br><%= link_to @user.name ,user_path(@user.id) %>
              </td>
              <td><%= @book.title %></td>
              <td><%= @book.body %></td>
              <% if @user.id == current_user.id %>
              <td><button type="button" class="btn btn-info btn-xs"><%= link_to 'Edit', edit_book_path(params[:id]) %></button></td>
              <td><button type="button" class="btn btn-danger btn-xs"><%= link_to 'Destroy', book_path(params[:id]), method: :delete, data: {confirm: "Are you sure?"} %></button></td>
              <% end %>
            </tr>
        </table>
      </div>
   </div>  
</div> 