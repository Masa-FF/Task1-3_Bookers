<div class="container"> 
    <div class="row"> 
      <div class="col-xs-3">
       <%= render 'users/info', user: @user, book: @newbook %>
      </div>
      <div class="col-xs-9"> 
        <table class="table table-basic">
          <tr>
            <th></th>
          　<th>Title</th>
            <th>opinion</th>
          </tr>
          <h3>Books</h3>
          <% @books.each do |book| %>
            <tr>
              <td><%= link_to(book.user) do %><%= attachment_image_tag book.user, :profile_image , :fill, 50, 50, format: "jpg", fallback: "no_image.jpg", size:'50x50' %><% end %></td>
              <td><%= link_to book.title, book_path(book.id) %></td>
              <td><%= book.body %></td>
            </tr>
          <% end %>  
        </table>
      </div>
   </div>  
</div>  